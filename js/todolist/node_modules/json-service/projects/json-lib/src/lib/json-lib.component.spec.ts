import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { JsonLibComponent } from './json-lib.component';

describe('JsonLibComponent', () => {
  let component: JsonLibComponent;
  let fixture: ComponentFixture<JsonLibComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ JsonLibComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(JsonLibComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
