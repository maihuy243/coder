import { NgModule } from '@angular/core';
import { JsonLibComponent } from './json-lib.component';

@NgModule({
  declarations: [JsonLibComponent],
  imports: [
  ],
  exports: [JsonLibComponent]
})
export class JsonLibModule { }
